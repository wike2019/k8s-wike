import { createStore } from 'vuex'

export  const store = createStore({
    state: {

    },
    mutations: {
        increment(state) {

        },
    },
    actions: {
        increment (context) {
           // context.commit('increment')
        }
    }
})